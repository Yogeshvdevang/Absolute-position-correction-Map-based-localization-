import { DroneSimView } from '@/components/DroneSimView';

const ProtocolSim = () => {
  return (
    <div className="h-screen w-screen bg-background">
      <DroneSimView />
    </div>
  );
};

export default ProtocolSim;
