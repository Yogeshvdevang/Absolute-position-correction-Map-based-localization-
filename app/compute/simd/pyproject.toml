[build-system]
requires = ["maturin>=1.5"]
build-backend = "maturin"

[project]
name = "mission_planner_simd"
version = "0.1.0"
requires-python = ">=3.9"

[tool.maturin]
bindings = "pyo3"
module-name = "mission_planner_simd"
