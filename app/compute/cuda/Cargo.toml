[package]
name = "mission_planner_cuda"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[features]
cuda = ["cudarc"]

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }
cudarc = { version = "0.11", optional = true }
